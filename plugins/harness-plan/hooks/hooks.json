{
  "description": "Harness v2 campaign resume context",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/session-start.sh",
            "timeout": 30
          }
        ],
        "description": "Inject compact .harness resume context when an active campaign exists"
      }
    ]
  }
}
