{
  "description": "Harness v2 campaign resume context + autodrive chain",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/session-start.sh",
            "timeout": 30
          }
        ],
        "description": "Inject compact .harness resume context when an active campaign exists"
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/stop.sh",
            "timeout": 15
          }
        ],
        "description": "Autodrive: spawn next session if a campaign is configured for cross-session auto-advance"
      }
    ]
  }
}
